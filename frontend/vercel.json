{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "comment": "Single-page app: every path must serve index.html, otherwise reloading on /foundations or /e-book returns a 404 from the CDN.",
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
